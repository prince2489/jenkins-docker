pipeline {
    agent any

    stages {
        stage('List files') {
            steps {
                sh '''
                ls
                pwd
                '''
            }
        }
        stage('Docker build'){
            steps {
                dir('simplenodeapp'){
                sh '''
                    docker build . -t nodejsapp
                '''
                }
            }
        }
        
    }
}
